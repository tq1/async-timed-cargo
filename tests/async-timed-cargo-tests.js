var chai    = require('chai'),
    expect  = chai.expect,
    assert  = chai.assert,
    asyncTimedCargo = require('../lib/async-timed-cargo')();

describe('async-timed-cargo', function() {

  beforeEach(function() {

  });

  it('it runs', function(done) {

    done();
  });

});
